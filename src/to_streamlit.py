import to_streamlit as st

st.set_page_config(
    page_title="Student Performance"), #<------- Change this to the page you're currently on when copying/pasting after your imports

st.subheader("Student Directory")
st.text("Queries and returns all information based on selected students.")